{
  "name": "@emotional-infrastructure/trust-receipt-demo-worker",
  "version": "0.2.0-demo.1",
  "private": true,
  "type": "module",
  "engines": { "node": ">=22.8" },
  "scripts": {
    "test": "node --test test/*.test.mjs",
    "dry-run": "wrangler deploy --dry-run --outdir dist",
    "check": "npm test && npm run dry-run",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "4.112.0"
  }
}
